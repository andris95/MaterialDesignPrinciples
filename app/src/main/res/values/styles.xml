<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Main" parent="AppTheme">
        <!--<item name="android:windowExitTransition">@transition/main_activity_exit_transition</item>-->
    </style>

    <style name="AppTheme.Details" parent="AppTheme">
        <item name="android:windowReturnTransition">@transition/details_return_transition</item>
        <item name="android:windowSharedElementEnterTransition">@transition/details_activity_shared_enter_transition</item>
    </style>

</resources>
